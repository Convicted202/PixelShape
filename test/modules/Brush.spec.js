import test from 'blue-tape';
// import Brush from 'modules/brush/Brush';

test('Tests run 2', (assert) => {
  // const brush = new Brush(),
  //       state = brush.state;

  // assert.equal(state.gridCellSize, 10, 'gridCellSize should be 10');
  assert.pass('Everything works fine');
  assert.end();
});
